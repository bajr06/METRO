#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_CARACTERES 20



int main(){

char palabra[MAX_CARACTERES];
scanf("%s",palabra);

for (int i = 0; i < MAX_CARACTERES; ++i){
	for (int i = 0; j < MAX_CARACTERES; ++j){
		do{

		}while(strcmp(palabra+j,(strlen(palabra-1)-i)) = 1);
	}
}

return 0; 
}